[package]
name = "calculator-engine"
version = "0.1.0"
edition = "2024"
rust-version = "1.95"
license = "MIT OR Apache-2.0"

[dependencies]
num-bigint = "0.4"
num-rational = "0.4"
num-complex = "0.4"
num-traits = "0.2"
astro-float = { version = "0.9.6", default-features = false, features = ["std"] }
serde = { version = "1.0.219", features = ["derive"] }

[lints]
workspace = true
