//! Workspace root library crate.
//!
//! The active application implementation lives in [`ui`]; this crate remains as
//! the package library target while the native/WASM bootstrap stays in `main.rs`.
